<h2>Produtos</h2>
<a href="/"><button class="main-btn">voltar</button></a>
<form id="editar-produto">
    <input type="hidden" id="inId" value="<%= produto.Prod_cod %>">
    <div>
        <label for="inNome">Nome:</label>
        <input type="text" id="inNome" required value="<%= produto.Prod_nome %>">
    </div>
    <div>
        <label for="inPreco">Preço:</label>
        <input type="number" id="inPreco" required value="<%= produto.Prod_preco %>">
    </div>
    <div>
        <label for="inQuantidade">Quantidade:</label>
        <input type="number" id="inQuantidade" required value="<%= produto.Prod_quantidade %>">
    </div>
    <div>
        <select name="inFornecedor" id="inFornecedor">
            <% fornecedores.forEach(fornecedor => { %>
                <option value="<%= fornecedor.Forn_id %>" 
                    <%= fornecedor.Forn_id === produto.Forn_id ? 'selected' : '' %>
                >
                    <%= fornecedor.Forn_nome %>
                </option>
            <% }) %>
        </select>
    </div>
    <div>
        <input type="submit" value="Editar" class="main-btn">
    </div>
</form>
<script src="/js/editarProduto.js"></script>
